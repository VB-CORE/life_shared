targets:
  $default:
    builders:
      json_serializable:json_serializable:
        options:
          # Models are written straight to Firestore, which takes a Map and
          # never runs jsonEncode. Without this, a nested model is emitted as a
          # raw Dart object and the write fails with "Invalid argument".
          explicit_to_json: true
